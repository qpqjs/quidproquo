/** The AWS identity env vars a deployment is delivered through, alongside core's `QpqDeployEnvVar`. */
export enum QpqAwsDeployEnvVar {
  accountId = 'AWS_DEFAULT_ACCOUNT',
  region = 'AWS_DEFAULT_REGION',
}
