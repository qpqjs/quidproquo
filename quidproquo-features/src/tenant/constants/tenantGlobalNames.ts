export const TENANT_HEADER_NAME_GLOBAL = 'tenantHeaderName';

// The header doccypoccy's frontend sends the user's selected tenant on.
export const DEFAULT_TENANT_HEADER_NAME = 'x-qpq-tenant-id';

// The merged role catalog (app roles + tenantAdmin), published by defineTenant in every service.
export const TENANT_ROLES_GLOBAL = 'tenantRoles';
